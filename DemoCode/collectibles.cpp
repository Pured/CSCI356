#include "stdafx.h"
#include "DemoApp.h"

DemoApp::Collectible::Collectible(int nodeNumber, int contents)
{
/*
	collectibleItem = mSceneMgr->createEntity(collectibleItem, "uv_sphere.mesh");
	collectibleItem->setMaterialName("Examples/droplet");

	collectibleNode = mSceneMgr->getRootSceneNode()->createChildSceneNode();
	collectibleNode->attachObject(temp.collectibleItem);
	collectibleNode->scale(0.1, 0.1, 0.1);

	position = pathFindingGraph->getPosition(nodeNumber);
	position.y = 5;
	collectibleNode->translate(temp.position);

	isVisible = true;
	timeTillSpawn = 0;
	collectibleType = contents;
*/
}